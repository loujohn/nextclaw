# v0.13.97 — 项目管理 Agent 多定时任务编排分析文档

## 迭代完成说明

本次迭代未修改数字员工调度代码，重点是补齐一个面向产品设计与二次开发的专项分析文档，回答“一个项目管理 agent 同时承担多个不同周期任务时，当前能力该如何使用，以及未来应如何改造”。

新增文档：

- [项目管理 Agent 多定时任务编排分析](../../internal/project-management-agent-multi-schedule-analysis.md)

文档重点覆盖：

- 当前数字员工自动化能力为什么不适合同一个员工原生挂 3 条独立任务
- 针对“每天提醒填写、每天下班前统计、每月初进度汇总”的推荐拆分方案
- 为什么推荐用“coordinator + 多个 cron worker”而不是一个大 agent
- `cron` 与 `heartbeat` 在这类场景下的合理分工
- 如何参考 OpenClaw-aligned 多 agent 思路做后续产品化改造
- 如果要真正支持“一个员工多任务定时”，数据库、调度和技能绑定应该如何演进

## 测试/验证/验收方式

本次为纯文档改动，`build`、`lint`、`tsc` 不适用，原因是未触达构建、类型或运行链路。

本次执行的最小验证：

1. 检查分析文档引用的现有代码文件与设计文档均存在
2. 检查新增迭代目录命名符合 `v<semver>-<slug>` 规范，且版本号高于现有最大版本
3. 检查迭代 README 包含四部分：迭代完成说明、测试/验证/验收方式、发布/部署方式、用户/产品视角的验收步骤

验收点：

- 阅读文档后，产品或研发能明确知道当前方案下应拆成几个 worker
- 能明确知道为什么这 3 类任务都应该优先用 `cron` 而不是 `heartbeat`
- 能明确知道未来若要支持“单员工多任务”，应优先改造哪些模型和表结构

## 发布/部署方式

本次为文档交付，无代码发布、无数据库 migration、无部署动作。

不适用项说明：

- NPM 发布：不适用
- 前端发布：不适用
- 远程 migration：不适用
- 线上冒烟：不适用

## 用户/产品视角的验收步骤

1. 打开 [项目管理 Agent 多定时任务编排分析](../../internal/project-management-agent-multi-schedule-analysis.md)
2. 查看“结论先行”与“当前版本最合理的使用方式”，确认推荐方案是 `1 个 coordinator + 3 个 worker`
3. 查看“为什么当前不适合同一个员工硬塞三个任务”，确认当前模型的核心限制是“一员工一条 schedule”
4. 查看“如果要改造功能，最值得怎么改”，确认后续演进方向包括：多 schedule job、任务级技能绑定、任务级 prompt 模板、supervisor-worker 编排
5. 结合你自己的项目管理场景，能直接按文档中的 cron 建议先落一版可执行方案