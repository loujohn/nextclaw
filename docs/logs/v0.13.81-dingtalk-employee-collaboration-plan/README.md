# v0.13.81-dingtalk-employee-collaboration-plan

## 迭代完成说明

- 新增钉钉数字员工协作能力的实施计划文档：[DingTalk Digital Employee Collaboration Implementation Plan](../../../superpowers/plans/2026-03-19-dingtalk-digital-employee-collaboration.md)
- 计划文档将方案拆成 4 个实现 Chunk：
  - 替换内置钉钉插件；
  - 接通数字员工 runtime 与多机器人配置；
  - 增加员工与群路由配置；
  - 暴露运营 UI 与协作约束。
- 每个 Chunk 均细化到具体文件、验证命令与预期结果，便于后续按阶段执行。

## 测试/验证/验收方式

- `build/lint/tsc`：不适用。本次仅新增计划文档与迭代记录，未触达代码路径。
- 文档验证：
  - 检查计划文档路径与链接有效；
  - 检查本次迭代目录命名符合 `v<semver>-<slug>`；
  - 检查版本号高于当前最大有效版本。
- 计划自审：
  - 每个任务是否给出明确文件范围；
  - 是否遵循“先私聊、再群聊、再多机器人、再协作”的实施顺序；
  - 是否明确保留 `dingtalk` channel id 并采用替换策略而非并存策略。

## 发布/部署方式

- 本次为计划文档迭代，不涉及代码发布。
- 后续进入实施阶段时，按计划文档中的 Chunk 顺序推进，并在每个实现迭代中补对应验证与冒烟记录。

## 用户/产品视角的验收步骤

1. 打开计划文档，确认实现顺序符合产品推进节奏：
   - 先替换钉钉插件；
   - 再接通 runtime 与多机器人；
   - 再接员工/群路由；
   - 最后补运营 UI 与协作保护。
2. 确认每个阶段都能独立交付、独立测试，而不是一次性大爆炸改造。
3. 确认计划中已经覆盖：
   - 私聊；
   - 群聊 `@机器人`；
   - 多机器人；
   - 员工后台协作；
   - 数字员工平台运营配置。
4. 若以上均符合预期，则以该计划作为正式实施拆分依据。
