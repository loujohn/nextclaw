# v0.13.96 — 数字员工自动化模块二开文档补齐

## 迭代完成说明

本次迭代未修改自动化运行逻辑本身，重点是把数字员工“自动化”模块的实现链路补成一份可直接供二次开发使用的技术文档，覆盖：

- 三种模式 `cron` / `every` / `heartbeat` 的设置方式
- 三种模式各自如何运行
- 自动化模块核心代码位置和阅读顺序
- 调度持久化、运行时装配、任务恢复、立即运行机制
- 三种模式的实现差异和二开注意事项

新增文档：

- [数字员工自动化模块二次开发说明](../../internal/digital-employee-automation-scheduling.md)

## 测试/验证/验收方式

本次变更为纯文档改动，`build`、`lint`、`tsc` 不适用，原因是未触达构建、类型或运行链路。

本次实际执行的最小验证：

1. 检查文档引用的核心源码文件均存在
2. 检查迭代目录命名符合 `v<semver>-<slug>` 规范
3. 检查 README 中包含必需四部分，并包含对独立技术文档的 Markdown 链接

验收点：

- 研发可以仅依据该文档定位到自动化模块核心文件
- 能明确区分 `cron`、`every`、`heartbeat` 三种模式的配置和运行差异
- 能明确知道 heartbeat 真实任务来源于 `HEARTBEAT.md`，而不是仅靠数据库字段

## 发布/部署方式

本次为文档交付，无代码发布、无数据库 migration、无部署动作。

不适用项说明：

- NPM 发布：不适用
- 前端发布：不适用
- 远程 migration：不适用
- 线上冒烟：不适用

## 用户/产品视角的验收步骤

1. 打开 [数字员工自动化模块二次开发说明](../../internal/digital-employee-automation-scheduling.md)
2. 查看“三种模式如何设置”章节，确认能找到 `cron`、`every`、`heartbeat` 的入口和参数说明
3. 查看“三种模式如何运行”章节，确认能顺着文档追到服务端调度、员工执行和运行记录落库链路
4. 查看“核心差异”章节，确认能快速判断不同模式适用场景和扩展方式
5. 查看“推荐阅读顺序”章节，确认后续二开时有清晰的源码阅读入口