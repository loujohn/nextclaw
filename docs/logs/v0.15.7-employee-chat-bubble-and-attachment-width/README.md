# 迭代完成说明

- 优化员工聊天页消息气泡宽度：用户消息与助手正文消息改为按内容自适应收缩，在保留最大宽度上限的同时去掉大段无意义留白。
- 优化聊天页文件胶囊样式：消息中的附件列表与输入框上方待发送文件都统一为固定视觉宽度，长文件名改为单行省略显示。
- 保持现有交互不变：附件入口、打开工作区、删除待发送文件等行为未改动。

# 测试/验证/验收方式

- VS Code 问题检查：已对 [packages/nextclaw-digital-employee/app/pages/employees/[id]/chat.vue](packages/nextclaw-digital-employee/app/pages/employees/%5Bid%5D/chat.vue) 执行 Problems 检查，无新增错误。
- 定向 lint：执行 `pnpm exec eslint app/pages/employees/'[id]'/chat.vue`，结果仅有该文件既有 `max-lines` warning，无本次改动新增 lint 错误。
- UI 冒烟：使用本地运行中的数字员工开发服务登录聊天页，进入员工 `gitlab` 的聊天页，验证结果如下。
- 冒烟观察点：
  - 用户短消息气泡宽度约 `64.7px`，其消息列宽度约 `116.3px`，确认气泡未再铺满整列。
  - 两个待发送文件胶囊宽度均为 `288px`，确认宽度统一。
  - 长文件名 `NextClaw数字员工平台-项目汇报.md` 的 `scrollWidth` 大于 `clientWidth`，确认发生省略截断。
  - 冒烟后已立即删除临时上传文件，页面恢复为无待发送文件状态。

# 发布/部署方式

- 本次仅涉及前端页面样式，无后端接口、数据库结构或构建链路变更。
- 按数字员工前端常规发布流程重新构建并部署 `@nextclaw/digital-employee` 即可，无需 migration。

# 用户/产品视角的验收步骤

1. 登录数字员工平台，进入任一员工详情页的聊天标签页。
2. 查看已有短消息或发送一条较短消息，确认消息气泡只包裹内容，不再出现明显横向空白。
3. 点击“上传文件”，选择两个不同长度文件名的文件，确认文件胶囊宽度一致。
4. 观察较长文件名，确认显示为单行且超出部分以省略号截断。
5. 删除待发送文件，确认删除流程与提示保持正常。