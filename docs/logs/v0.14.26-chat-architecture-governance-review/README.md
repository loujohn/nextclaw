# v0.14.26 chat architecture governance review

## 迭代完成说明

本次迭代重审并重写了 `docs/superpowers/specs/2026-04-08-chat-architecture-governance-design.md`，重点调整如下：

1. 删除第六章中“同步版 + 流式版”双轨设计，收敛为单一路径的 SSE 流式执行与服务端取消方案。
2. 将第七章历史读取策略统一为按 `sessionKey` 查询，不再把内存命中/恢复过程写成主设计。
3. 明确平台现有数据库已承载 `run_records / run_events`，并新增聊天会话/消息入库设计以支撑多会话、历史、分页能力。
4. 明确本轮不处理改造前旧历史迁移，删除过重的迁移与适配器叙事。

## 测试/验证/验收方式

1. 人工校对文档章节，确认第六章、第七章、第八章与最终结论一致。
2. 检查文档中不再包含“同步版兼容主线”“按内存命中决定历史读取主路径”“旧历史迁移为本轮必做”等表述。
3. 对照当前后端代码，确认文档中的现状判断与接口收敛方向一致。

## 发布/部署方式

本次仅更新设计文档与迭代记录，无需发布或部署。

## 用户/产品视角的验收步骤

1. 阅读 `docs/superpowers/specs/2026-04-08-chat-architecture-governance-design.md` 的第五至第八章。
2. 确认文档已明确多会话以 `sessionKey` 为核心、流式执行以 SSE 为主、历史读取统一按 `sessionKey` 查询。
3. 确认文档已承认现有数据库存在，并新增聊天会话/消息表设计，同时排除旧历史迁移范围。
