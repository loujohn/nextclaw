# v0.15.17 chat tool output full display

## 迭代完成说明

- 排查确认：聊天页执行时间线中的工具调用与工具结果内容，接口返回是完整的，前端展示层额外调用了截断函数，导致文本尾部被替换成省略号。
- 修复方式：移除工具调用/工具结果代码块展示中的前端 `truncateStr` 截断，改为完整渲染接口返回内容。
- 保留原有滚动容器与最大高度，避免长输出把消息卡片整体撑爆。

## 测试/验证/验收方式

- 执行：`tests/chat-message-groups.test.ts`，确认聊天时间线相关现有分组逻辑未被破坏。
- 执行：VS Code 问题检查，确认 `app/pages/employees/[id]/chat.vue` 无新增错误。
- 未执行全量 `build/lint/tsc`：本次为聊天页单点展示逻辑修复，采用定向测试 + 问题检查作为最小充分验证。

## 发布/部署方式

- 本次仅涉及 `packages/nextclaw-digital-employee` 聊天页前端展示代码，无 migration、无后端链路变化。
- 按现有数字员工平台常规前端部署流程发布即可。

## 用户/产品视角的验收步骤

1. 在聊天页触发一轮包含工具调用和工具结果的对话。
2. 展开“执行时间线”，观察工具调用参数与工具结果输出。
3. 确认代码块内容完整显示，不再因为前端截断而出现省略号。
4. 当内容较长时，确认卡片内部仍可滚动查看全部内容，整体布局不被撑坏。