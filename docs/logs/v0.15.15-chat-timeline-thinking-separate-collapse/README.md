# v0.15.15 chat timeline thinking separate collapse

## 迭代完成说明

- 聊天页执行时间线中，`thinking` 事件不再覆盖到同一个“思考过程”模块，而是按收到的事件顺序逐条追加显示。
- 执行时间线模块新增“展开/收起”能力，默认展开，用户可按消息维度独立折叠。
- 保留累计 `reasoning` 文本用于状态与语义连续性，但时间线展示改为事件级粒度。

## 测试/验证/验收方式

- 执行：`tests/chat-message-groups.test.ts`，确认聊天消息分组与时间线顺序未被破坏。
- 执行：VS Code 问题检查，确认 `app/pages/employees/[id]/chat.vue` 无新增错误。
- 未执行全量 `build/lint/tsc`：本次为聊天页局部交互与展示逻辑改动，采用定向单测 + 问题检查作为最小充分验证。

## 发布/部署方式

- 本次仅涉及 `packages/nextclaw-digital-employee` 聊天页前端代码，无 migration、无后端发布链路变化。
- 按现有数字员工平台常规前端部署流程发布即可。

## 用户/产品视角的验收步骤

1. 在员工聊天页触发一轮包含多次 thinking 事件的对话。
2. 观察“执行时间线”，确认多条 thinking 内容按时间顺序分别展示，而不是合并在同一节点内。
3. 点击执行时间线头部的“收起/展开”按钮，确认模块可折叠并可再次展开。