# 元工（MetaWorker）平台 v0.15 迭代测试报告

> 测试周期：2026-04-10 ~ 2026-04-24
> 涉及代码变更：319 文件，+35182 / -2208 行

---

## 一、功能点清单

### 1. 用户认证与权限管理（RBAC）

| 功能点 | 说明 |
|---|---|
| 本地密码登录 | bcryptjs 哈希 + JWT（jose）签发，支持 token 刷新 |
| Keycloak SSO 登录 | PKCE 流程，OAuth callback 页面 |
| 双模式登录页 | SSO 与密码登录双入口，登录失效统一跳转 |
| 用户管理 CRUD | 用户列表、创建、编辑、删除、重置密码 |
| RBAC 权限守卫 | admin-only 接口权限校验，服务端 auth middleware |
| 退出登录修复 | 服务端 cookie 清理，会话失效统一处理 |

### 2. 人员同步模块

| 功能点 | 说明 |
|---|---|
| 人员同步基础功能 | 从统一登录系统同步人员到本地 users 表 |
| 批量同步更新 | 人员同步改为批量更新，减少 API 调用 |
| 自动关联 | 同步人员时自动关联钉钉 ID，处理前导零问题 |
| 同步进度展示 | 前端同步进度弹窗，实时显示同步状态 |
| 同步结果弹窗 | 同步完成后展示成功/失败统计 |
| 同步错误提示 | 优化同步失败时的错误信息展示 |
| keycloak_sub 唯一索引 | 修复 keycloak_sub 字段唯一索引冲突问题 |

### 3. 技能管理体系

| 功能点 | 说明 |
|---|---|
| 技能版本追踪 | 员工技能表新增 version 字段 |
| 技能单个升级 | 员工卡片支持单个技能升级，弹窗确认 |
| 技能批量升级 | 一键升级所有可升级技能 |
| 升级进度与错误提示 | 前端展示升级进度条和错误信息 |
| 全局技能删除 | 支持删除全局技能，员工侧暴露 installMissing 状态 |
| 已删除警告标签 | 技能全局删除时展示红色「已删除」警告标签 |
| 自动补全 version | 技能导入时自动检测并补全 SKILL.md 中缺失的 version 字段 |
| CRLF 兼容修复 | SKILL.md frontmatter 解析兼容 CRLF 换行 |
| 全局技能分页 | 技能中心页面支持分页展示 |
| 员工技能分页 | 员工技能列表支持分页展示 |
| 技能编辑面板优化 | 内嵌切入面板，只展示未绑定技能，带搜索/分类筛选/分页 |
| seed-skills 保护 | 跳过用户手动导入的技能，避免重启覆盖 |

### 4. 日报/周报技能

| 功能点 | 说明 |
|---|---|
| 日报填写技能 | 新增日报填写技能，支持项目查询、内容优化规则 |
| 周报填写技能 | 周报填写对接项管接口 |
| 版本号控制 | 日报/周报添加版本号控制 |
| 文件目录优化 | 优化日报/周报文件存储目录结构 |
| 多用户提交 | 单个助手为所有用户提交日报 |
| 问题与风险字段 | 日报新增问题与风险/请示事项字段 |
| 无项目友好提示 | 无项目情况下显示友好提示 |
| 项目查询分用户存储 | 日报项目查询按用户隔离存储 |

### 5. 聊天模块增强

| 功能点 | 说明 |
|---|---|
| 多用户消息增强 | 身份感知、定向发送、群名解析 |
| IdentityResolver | AI 能识别消息发送者身份（姓名/部门/职位） |
| MessageTool 扩展 | to_name、to_user、to_group、to_group_name、mention、account_id |
| 群名 DB 持久化 | channel_groups 表，支持精确和模糊匹配 |
| DingTalk @mention | @提及支持（视觉效果） |
| 聊天页面初始化修复 | 修复聊天页面初始化错误 |
| 聊天内容显示优化 | 修复内容显示错位，优化聊天结果显示 |
| 聊天模块 bug 修复 | 多项聊天模块 bug 修复 |
| 文件上传模块修复 | 上传文件模块 bug 修复 |
| 工作空间代码预览 | 工作空间增加对代码文件的预览和编辑 |
| 聊天命令增强 | 增强数字员工聊天命令处理 |
| 去掉轮询 | 去掉聊天页面轮询机制 |
| 同步接口错误提示 | 修复同步接口错误提示 |

### 6. 会话隔离与 agentId 作用域

| 功能点 | 说明 |
|---|---|
| SessionsListTool agentId 隔离 | 会话列表按 agentId 过滤 |
| SessionsHistoryTool agentId 隔离 | 会话历史按 agentId 过滤 |
| sessions_send 员工收件箱 | 仅给 agentId 时投递到员工收件箱 |
| 员工收件箱路由 | 按 employee code 直接路由渠道消息 |
| setSessionsToolContext | 注入 agentId 到 sessions list/history tools |

### 7. 工作空间安全

| 功能点 | 说明 |
|---|---|
| restrictToWorkspace 强制 | 员工引擎强制限制文件操作在工作空间内 |
| 读操作路径放行 | restrictToWorkspace 只检查写操作目标路径，不拦截读命令的绝对路径参数 |
| SOUL.md 支持 | 员工工作空间包含 SOUL.md 及相关功能增强 |

### 8. Webhook 触发器

| 功能点 | 说明 |
|---|---|
| 通用 Webhook 触发器 | 替代硬编码 GitLab webhook |
| employee webhook migration | 新增员工 webhook 数据库迁移 |
| GitLab Code Review 技能 | 新增 GitLab Code Review 技能与 Webhook 端点 |
| 无头触发通知 | 无头触发自动注入渠道通知提示 |

### 9. 数据库与基础设施

| 功能点 | 说明 |
|---|---|
| DM-only 迁移 | 移除 SQLite，采用达梦数据库专属 Knex 迁移 |
| Knex 最佳实践 | Knex.js migration 最佳实践改进 |
| 连接池断连修复 | 修复达梦数据库连接池断连问题 |
| 环境文件管理 | 启动时读取 env 文件，部署复制 env 环境 |
| Dockerfile 优化 | 修改 Dockerfile 和入口启动文件命令 |
| CI/CD 更新 | 更新 .gitlab-ci.yml 文件 |
| knex-dm 依赖 | 添加 knex-dm 运行时依赖安装 |
| 平台名称变更 | 平台名称从"数字员工平台"改为"元工（MetaWorker）" |

### 10. 定时任务与调度

| 功能点 | 说明 |
|---|---|
| 平台运行时感知 | 增加平台运行时感知与调度工具支持 |
| 定时任务 bug 修复 | 修复定时任务相关 bug |
| 调度目标计划 | 定时任务分发目标优化 |
| delivery_targets 移除 | 完全移除 delivery_targets 机制 |

### 11. 资源审计字段

| 功能点 | 说明 |
|---|---|
| 资源审计用户字段 | 员工、会话、定时任务创建绑定当前用户 |
| 排除归档员工 | 从技能使用情况统计中排除已归档员工 |

---

## 二、影响范围

### 2.1 受影响模块

| 模块 | 影响程度 | 说明 |
|---|---|---|
| `packages/nextclaw-digital-employee` | **高** | 核心变更模块，包含前端、后端、数据库迁移、技能、服务层 |
| `packages/nextclaw-core` | **中** | agent tools（sessions-list/send）、shell restrictToWorkspace、消息工具扩展 |
| `packages/extensions/nextclaw-channel-plugin-dingtalk` | **中** | 渠道消息路由、@mention 支持、消息标准化 |
| 数据库（达梦） | **高** | 新增 12 个 migration，表结构变更（users、channel_groups、skill version 等） |
| Docker/CI-CD | **中** | Dockerfile 变更、build.sh 变更、.gitlab-ci.yml 变更 |

### 2.2 数据库变更

| Migration | 变更内容 | 影响 |
|---|---|---|
| 004_users_table | 新增 users 表 | 认证系统基础 |
| 005_users_local_auth | 本地认证字段 | 密码登录支持 |
| 006_users_username | 用户名字段 | 用户名展示 |
| 007_seed_default_admin | 默认管理员种子数据 | 初始管理员账户 |
| 008_chat_sessions_messages | 聊天会话和消息表 | 聊天持久化 |
| 009_employee_webhook | 员工 webhook 配置表 | Webhook 功能 |
| 010_employee_skill_version | 技能版本字段 | 技能版本追踪 |
| 011_users_personnel_sync | 人员同步相关字段 | 人员同步功能 |
| 012_resource_audit_user_fields | 资源审计字段 | 操作审计追踪 |
| channel_groups 表 | 群名持久化表 | 多用户消息群名解析 |

### 2.3 API 变更

| API 端点 | 变更类型 | 说明 |
|---|---|---|
| `POST /api/auth/login` | 新增 | 本地密码登录 |
| `POST /api/auth/logout` | 新增 | 退出登录 |
| `GET /api/auth/me` | 新增 | 获取当前用户信息 |
| `POST /api/auth/token` | 新增 | Token 刷新 |
| `GET /api/users` | 新增 | 用户列表（分页） |
| `POST /api/users/create` | 新增 | 创建用户 |
| `PATCH /api/users/:id` | 新增 | 更新用户 |
| `DELETE /api/users/:id` | 新增 | 删除用户 |
| `POST /api/users/:id/reset-password` | 新增 | 重置密码 |
| `POST /api/users/sync-trigger` | 新增 | 触发人员同步 |
| `GET /api/users/sync-status/:jobId` | 新增 | 查询同步状态 |
| `POST /api/employees/:id/skills/:skillName/upgrade` | 新增 | 技能升级 |
| `DELETE /api/skills/:name` | 新增 | 删除全局技能 |
| `GET /api/employees/:id/identity` | 新增 | 获取员工身份信息 |
| `GET /api/employees/:id/edit` | 新增 | 获取员工编辑数据 |
| `POST /api/webhooks/e/:code` | 新增 | 通用 Webhook 端点 |
| `GET /api/employees/:id/workspace/file` | 新增 | 工作空间文件读取 |
| `PUT /api/employees/:id/workspace/file` | 新增 | 工作空间文件写入 |
| `GET /api/employees/:id/workspace/raw` | 新增 | 工作空间原始文件 |
| `GET /api/employees/:id/workspace/uploaded` | 新增 | 上传文件列表 |

### 2.4 前端页面变更

| 页面 | 变更内容 |
|---|---|
| `/login` | 新增登录页面（SSO + 密码双模式） |
| `/auth/callback` | 新增 OAuth 回调页面 |
| `/employees` | 员工列表页面（技能升级、删除标签） |
| `/employees/:id` | 员工详情（技能版本、升级弹窗） |
| `/employees/:id/chat` | 聊天页面（多用户消息、初始化修复） |
| `/employees/:id/config` | 员工配置页面 |
| `/employees/:id/workspace` | 工作空间（代码预览/编辑） |
| `/employees/:id/jobs` | 定时任务页面 |
| `/skills/:category` | 技能中心（分页展示） |
| `/users` | 用户管理（列表、同步、CRUD） |

---

## 三、测试范围

### 3.1 功能测试范围

| 优先级 | 测试模块 | 测试重点 |
|---|---|---|
| P0 | 用户认证与登录 | 密码登录、SSO 登录、token 刷新、退出登录、登录失效跳转 |
| P0 | 人员同步 | 同步触发、进度展示、结果校验、自动关联、错误处理 |
| P0 | 技能版本升级 | 单个升级、批量升级、升级冲突、CRLF 兼容、缺失 version 补全 |
| P0 | 聊天核心功能 | 消息发送/接收、多用户身份识别、文件上传、会话隔离 |
| P0 | 数据库迁移 | 所有 12 个 migration 执行、幂等性、回滚 |
| P1 | 日报/周报技能 | 日报填写、周报填写、版本控制、无项目提示 |
| P1 | 工作空间安全 | restrictToWorkspace 写操作拦截、读操作放行、代码预览 |
| P1 | Webhook 触发器 | 通用 webhook 接收、GitLab code review、无头通知 |
| P1 | 用户管理 | 用户 CRUD、权限校验、删除优化、密码重置 |
| P2 | 定时任务 | 任务创建、执行、分发目标、历史回放 |
| P2 | 资源审计 | 创建者字段、归档员工排除 |
| P2 | 钉钉渠道 | @mention、群名解析、消息标准化 |

### 3.2 兼容性测试范围

| 测试项 | 说明 |
|---|---|
| 浏览器兼容 | Chrome、Firefox、Safari、Edge 最新版 |
| 数据库兼容 | 达梦数据库（开发/测试/生产环境） |
| 操作系统 | macOS、Linux、Windows（CLI 相关功能） |

### 3.3 安全测试范围

| 测试项 | 说明 |
|---|---|
| 认证安全 | JWT 签名验证、token 过期处理、cookie 安全属性 |
| 权限隔离 | 非 admin 用户无法访问管理接口、agentId 会话隔离 |
| 工作空间安全 | restrictToWorkspace 防止越权文件操作 |
| SQL 注入 | 达梦数据库查询参数化 |

---

## 四、测试用例

### 4.1 用户认证与权限管理

#### TC-AUTH-001: 本地密码登录 - 成功
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 访问 `/login` 页面 | 显示登录表单（用户名 + 密码 + SSO 按钮） |
| 2 | 输入正确的用户名和密码 | — |
| 3 | 点击登录按钮 | 跳转到首页，显示用户信息 |
| 4 | 检查 token | localStorage/cookie 中存在 JWT token |

#### TC-AUTH-002: 本地密码登录 - 失败
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 访问 `/login` 页面 | — |
| 2 | 输入错误的密码 | — |
| 3 | 点击登录按钮 | 显示错误提示，不跳转 |

#### TC-AUTH-003: Keycloak SSO 登录
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 访问 `/login` 页面 | — |
| 2 | 点击 SSO 登录按钮 | 跳转到 Keycloak 登录页 |
| 3 | 在 Keycloak 完成登录 | 回调到 `/auth/callback`，自动跳转首页 |
| 4 | 检查用户信息 | 用户信息正确，包含 keycloak_sub |

#### TC-AUTH-004: Token 刷新
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 登录后等待 token 接近过期 | — |
| 2 | 发起任意 API 请求 | token 自动刷新，请求成功 |

#### TC-AUTH-005: 退出登录
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 已登录状态下点击退出 | — |
| 2 | 检查 cookie | 认证 cookie 被清除 |
| 3 | 访问受保护页面 | 跳转到登录页 |

#### TC-AUTH-006: 登录失效统一跳转
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 登录后 token 过期 | — |
| 2 | 访问任意受保护页面 | 自动跳转到 `/login` |
| 3 | 登录后 | 回到原页面或首页 |

#### TC-AUTH-007: RBAC 权限校验 - 非 admin 用户
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 使用非 admin 用户登录 | — |
| 2 | 尝试访问用户管理 API | 返回 403 Forbidden |
| 3 | 尝试访问用户列表页面 | 页面不显示或显示无权限提示 |

#### TC-AUTH-008: RBAC 权限校验 - admin 用户
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 使用 admin 用户登录 | — |
| 2 | 访问用户管理 API | 返回 200，数据正常 |
| 3 | 访问用户列表页面 | 页面正常展示 |

### 4.2 人员同步模块

#### TC-SYNC-001: 触发人员同步
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在用户管理页面点击「同步人员」 | 弹出同步进度弹窗 |
| 2 | 观察进度条 | 进度条从 0% 逐步增长到 100% |
| 3 | 同步完成 | 弹出同步结果弹窗，显示成功/失败数量 |

#### TC-SYNC-002: 同步结果验证
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 同步完成后查看用户列表 | 新增的用户显示在列表中 |
| 2 | 检查用户字段 | 用户名、部门、职位等字段正确 |
| 3 | 检查钉钉 ID 关联 | 钉钉 ID 正确关联（含前导零处理） |

#### TC-SYNC-003: 同步自动关联
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 同步前存在本地用户（无钉钉 ID） | — |
| 2 | 执行同步 | — |
| 3 | 检查用户详情 | 钉钉 ID 自动关联成功 |

#### TC-SYNC-004: 同步错误处理
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 配置错误的同步 API 地址 | — |
| 2 | 触发同步 | 显示明确的错误提示信息 |
| 3 | 检查系统状态 | 系统正常运行，未崩溃 |

#### TC-SYNC-005: keycloak_sub 唯一索引
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 同步包含重复 keycloak_sub 的用户 | — |
| 2 | 检查同步结果 | 不报唯一索引冲突错误，正确处理 |

### 4.3 技能管理体系

#### TC-SKILL-001: 技能版本显示
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 打开员工详情页面 | — |
| 2 | 查看技能列表 | 每个技能显示版本号 |

#### TC-SKILL-002: 单个技能升级
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在员工技能列表中找到有新版本的技能 | — |
| 2 | 点击升级按钮 | 弹出确认弹窗 |
| 3 | 确认升级 | 显示升级进度，完成后版本号更新 |

#### TC-SKILL-003: 批量技能升级
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在员工详情页面点击「全部升级」 | 弹出确认弹窗，显示可升级数量 |
| 2 | 确认升级 | 逐个升级，显示进度条 |
| 3 | 升级完成 | 所有技能版本号更新，显示成功提示 |

#### TC-SKILL-004: 技能升级错误处理
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 升级一个存在冲突的技能 | — |
| 2 | 观察结果 | 显示具体错误信息，其他技能继续升级 |

#### TC-SKILL-005: 全局技能删除
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在技能中心删除一个全局技能 | — |
| 2 | 查看已绑定该技能的员工卡片 | 显示红色「已删除」警告标签 |
| 3 | 检查 installMissing 状态 | 员工侧正确暴露缺失状态 |

#### TC-SKILL-006: 技能导入自动补全 version
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 导入一个 SKILL.md 中没有 version 字段的技能 | — |
| 2 | 检查导入结果 | 自动补全 version 字段，默认值为 1.0.0 |

#### TC-SKILL-007: CRLF 兼容
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 上传一个使用 CRLF 换行的 SKILL.md | — |
| 2 | 检查版本解析 | 版本号正确解析，不被重置为 1.0.0 |

#### TC-SKILL-008: 技能分页
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 访问技能中心页面 | 技能按分页展示 |
| 2 | 翻页 | 正确显示下一页数据 |
| 3 | 访问员工技能列表 | 员工技能按分页展示 |

#### TC-SKILL-009: 技能编辑面板
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在员工配置页面点击添加技能 | 内嵌切入选择面板 |
| 2 | 检查面板内容 | 只展示未绑定的技能 |
| 3 | 使用搜索功能 | 搜索结果正确过滤 |
| 4 | 使用分类筛选 | 按分类正确过滤 |
| 5 | 勾选技能后点击「添加 N 个技能」 | 技能添加成功，回到主视图 |
| 6 | 面板打开期间 | 「上一步/下一步」导航栏隐藏 |

#### TC-SKILL-010: seed-skills 保护
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 手动导入一个技能 | — |
| 2 | 重启服务 | — |
| 3 | 检查手动导入的技能 | 未被覆盖，保持用户修改 |

### 4.4 日报/周报技能

#### TC-REPORT-001: 日报填写
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 对员工发送日报填写指令 | — |
| 2 | 检查日报内容 | 包含今日工作内容、问题与风险/请示事项 |
| 3 | 检查文件生成 | 日报文件正确生成到指定目录 |

#### TC-REPORT-002: 周报填写
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 对员工发送周报填写指令 | — |
| 2 | 检查周报内容 | 对接项管接口数据正确 |
| 3 | 检查文件生成 | 周报文件正确生成 |

#### TC-REPORT-003: 无项目友好提示
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 对无项目关联的用户触发日报 | — |
| 2 | 检查结果 | 显示友好提示而非报错 |

#### TC-REPORT-004: 版本控制
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 提交日报 | — |
| 2 | 检查文件名/内容 | 包含版本号信息 |

#### TC-REPORT-005: 多用户提交
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 单个助手为多个用户提交日报 | — |
| 2 | 检查每个用户的日报 | 各用户日报内容独立正确 |

### 4.5 聊天模块

#### TC-CHAT-001: 消息发送与接收
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在员工聊天页面发送消息 | — |
| 2 | 检查消息显示 | 消息正确显示在聊天窗口 |
| 3 | 等待员工回复 | 回复消息正确显示 |

#### TC-CHAT-002: 多用户身份识别
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 多人在群聊中发送消息 | — |
| 2 | 检查 AI 回复 | AI 能正确识别每条消息的发送者（姓名/部门/职位） |

#### TC-CHAT-003: 定向发送（私聊）
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 使用 to_name 指令按姓名私聊 | 消息仅发送给指定用户 |
| 2 | 使用 to_user 指令按 ID 私聊 | 消息仅发送给指定用户 |

#### TC-CHAT-004: 群消息发送
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 使用 to_group/to_group_name 发送群消息 | 消息发送到指定群 |
| 2 | 使用模糊群名匹配 | 正确解析到目标群 |

#### TC-CHAT-005: @mention 功能
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在消息中使用 @提及 | — |
| 2 | 检查消息显示 | 钉钉渠道显示 @视觉效果 |

#### TC-CHAT-006: 聊天页面初始化
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 打开员工聊天页面 | — |
| 2 | 检查初始化状态 | 无初始化错误，正常加载历史消息 |

#### TC-CHAT-007: 文件上传
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在聊天中上传文件 | — |
| 2 | 检查文件显示 | 文件正确显示在聊天中 |
| 3 | 点击文件预览 | 文件预览正常 |

#### TC-CHAT-008: 会话隔离（agentId）
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工 A 查看会话列表 | 只看到员工 A 的会话 |
| 2 | 员工 B 查看会话列表 | 只看到员工 B 的会话 |
| 3 | 检查会话历史 | 各员工只能查看自己的会话历史 |

#### TC-CHAT-009: sessions_send 投递
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 仅给 agentId 调用 sessions_send | 消息投递到员工收件箱 |
| 2 | 检查收件箱 | 消息正确到达 |

### 4.6 工作空间安全

#### TC-WORKSPACE-001: restrictToWorkspace 写操作拦截
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工引擎尝试在工作空间外创建文件 | — |
| 2 | 检查结果 | 操作被拦截，返回错误 |

#### TC-WORKSPACE-002: restrictToWorkspace 读操作放行
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工引擎读取工作空间外的文件（只读） | — |
| 2 | 检查结果 | 读取操作正常执行，不被拦截 |

#### TC-WORKSPACE-003: 代码文件预览
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在工作空间页面点击代码文件 | — |
| 2 | 检查预览 | 代码文件内容正确预览，带语法高亮 |

#### TC-WORKSPACE-004: 代码文件编辑
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在工作空间页面编辑代码文件 | — |
| 2 | 保存修改 | 修改成功保存 |
| 3 | 重新打开文件 | 显示修改后的内容 |

#### TC-WORKSPACE-005: SOUL.md 支持
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 检查员工工作空间 | 包含 SOUL.md 文件 |
| 2 | 修改 SOUL.md | 修改生效，员工行为相应变化 |

### 4.7 Webhook 触发器

#### TC-WEBHOOK-001: 通用 Webhook 接收
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 向 `/api/webhooks/e/:code` 发送 POST 请求 | — |
| 2 | 检查响应 | 返回 200，触发对应员工任务 |

#### TC-WEBHOOK-002: GitLab Code Review
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | GitLab 推送 MR 事件到 webhook | — |
| 2 | 检查员工响应 | 员工执行 Code Review 并返回结果 |

#### TC-WEBHOOK-003: 无头触发通知
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 无头模式触发员工任务 | — |
| 2 | 检查渠道通知 | 自动注入通知提示到对应渠道 |

### 4.8 用户管理

#### TC-USER-001: 用户列表分页
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 访问用户管理页面 | 用户列表分页展示 |
| 2 | 翻页 | 正确加载下一页数据 |
| 3 | 搜索用户 | 搜索结果正确 |

#### TC-USER-002: 创建用户
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 点击创建用户 | — |
| 2 | 填写用户信息并提交 | 用户创建成功 |
| 3 | 检查用户列表 | 新用户出现在列表中 |

#### TC-USER-003: 删除用户
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 选择一个用户点击删除 | 弹出确认弹窗 |
| 2 | 确认删除 | 用户删除成功 |
| 3 | 检查用户列表 | 用户不再显示 |
| 4 | 检查关联数据 | 该用户的技能使用情况已同步清理 |

#### TC-USER-004: 重置密码
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 选择用户点击重置密码 | — |
| 2 | 输入新密码 | 密码重置成功 |
| 3 | 使用新密码登录 | 登录成功 |

### 4.9 数据库迁移

#### TC-DB-001: Migration 执行
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在干净数据库执行所有 migration | — |
| 2 | 检查执行结果 | 所有 migration 成功执行，无错误 |
| 3 | 检查表结构 | 所有表正确创建，字段类型正确 |

#### TC-DB-002: Migration 幂等性
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 重复执行已完成的 migration | — |
| 2 | 检查结果 | 不报错，不重复创建表/字段 |

#### TC-DB-003: 达梦数据库连接
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 启动服务连接达梦数据库 | — |
| 2 | 检查连接池 | 连接正常，无断连报错 |
| 3 | 执行查询操作 | 查询正常返回结果 |

### 4.10 定时任务

#### TC-SCHEDULE-001: 定时任务创建
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在员工定时任务页面创建任务 | — |
| 2 | 检查任务列表 | 任务正确显示，包含创建者信息 |

#### TC-SCHEDULE-002: 定时任务执行
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 等待定时任务触发 | — |
| 2 | 检查执行结果 | 任务正确执行，结果记录到历史 |

#### TC-SCHEDULE-003: 定时任务分发
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 创建带分发目标的定时任务 | — |
| 2 | 任务触发后 | 消息正确分发到目标 |

### 4.11 钉钉渠道

#### TC-DINGTALK-001: 消息标准化
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉发送消息到员工 | — |
| 2 | 检查消息格式 | 消息正确标准化处理 |

#### TC-DINGTALK-002: 员工收件箱路由
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉渠道消息到达 | — |
| 2 | 检查路由 | 消息按 employee code 正确路由到收件箱 |

### 4.12 Docker/部署

#### TC-DEPLOY-001: Docker 构建
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 执行 Docker 构建 | — |
| 2 | 检查构建结果 | 构建成功，无错误 |
| 3 | 启动容器 | 容器正常启动，服务可访问 |

#### TC-DEPLOY-002: 环境文件加载
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 启动服务 | — |
| 2 | 检查环境变量 | 环境文件正确加载 |
| 3 | 检查 NEXTCLAW_SECRET_KEY | 密钥正确配置 |

#### TC-DEPLOY-003: knex-dm 依赖
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 检查 Docker 镜像 | — |
| 2 | 验证 knex-dm 驱动 | 驱动正确安装，可连接达梦数据库 |

---

## 五、测试环境要求

| 环境 | 配置要求 |
|---|---|
| 数据库 | 达梦数据库（与生产环境版本一致） |
| Node.js | 项目指定版本（参考 package.json engines） |
| 浏览器 | Chrome 最新版（主要测试浏览器） |
| 钉钉 | 测试用钉钉应用配置 |
| Keycloak | 测试用 Keycloak 实例（可选，用于 SSO 测试） |

## 六、测试数据准备

| 数据 | 说明 |
|---|---|
| 默认管理员账户 | 由 007_seed_default_admin migration 自动创建 |
| 测试用户 | 通过人员同步或手动创建至少 3 个测试用户 |
| 测试员工 | 创建至少 2 个测试员工，分别绑定不同技能 |
| 测试技能 | 确保技能中心有足够多的技能用于分页/搜索/筛选测试 |
| 钉钉测试群 | 用于多用户消息和 @mention 测试 |

## 七、已知风险与注意事项

| 风险项 | 说明 | 应对 |
|---|---|---|
| 达梦数据库兼容性 | 部分 migration 使用 dbNow() 等达梦特定函数 | 确保测试环境使用达梦数据库 |
| 人员同步依赖外部系统 | 同步功能依赖统一登录系统可用性 | 准备 mock 或测试环境同步源 |
| Keycloak SSO 依赖 | SSO 测试需要 Keycloak 实例 | 如无 Keycloak 可跳过 SSO 测试，仅测密码登录 |
| 钉钉渠道测试 | 需要真实的钉钉应用配置 | 准备测试用钉钉应用 |
| 数据迁移不可逆 | 部分 migration 包含数据变更 | 测试前备份数据库 |

---

## 八、智能体运行时层测试用例（Agent Runtime Level）

> **重要性**：以下测试验证 AI 智能体本身的运行时行为，而非 API/UI 层。这些是本轮迭代中最核心但最容易被遗漏的测试。

### 8.1 智能体工具调用能力（AI 能否正确使用工具）

#### TC-AGENT-001: MessageTool — to_name 单匹配
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在对话中让 AI "给张三发消息" | AI 调用 `message` 工具，`to_name: "张三"` |
| 2 | IdentityResolver 返回唯一匹配 | 消息正确发送到张三 |
| 3 | 检查 metadata | `metadata.target_user_id` 已设置 |

#### TC-AGENT-002: MessageTool — to_name 多匹配（歧义处理）
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在对话中让 AI "给王伟发消息"（存在多个王伟） | AI 调用 `message` 工具 |
| 2 | IdentityResolver 返回多个候选 | 工具返回错误，列出候选列表（姓名+部门） |
| 3 | 检查 AI 行为 | AI 应追问用户确认具体是哪个王伟，然后使用 `to_user` |

#### TC-AGENT-003: MessageTool — to_name 无匹配
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 在对话中让 AI "给不存在的名字发消息" | AI 调用 `message` 工具 |
| 2 | IdentityResolver 返回 null | 工具返回错误"未找到该用户" |
| 3 | 检查 AI 行为 | AI 告知用户找不到该人 |

#### TC-AGENT-004: MessageTool — to_group_name 精确+模糊匹配
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 让 AI "在技术部群里发通知" | AI 调用 `message` 工具，`to_group_name: "技术部群"` |
| 2 | 群名精确匹配 | 消息发送到对应群 |
| 3 | 让 AI "在技术群里发通知"（模糊匹配） | 消息通过 LIKE 模糊匹配发送到对应群 |

#### TC-AGENT-005: MessageTool — mention 混合解析
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 让 AI "在群里 @张三 @李四 通知一下" | AI 调用 `message` 工具，`mention: ["张三", "李四"]` |
| 2 | 检查解析 | 姓名正确解析为用户 ID，`metadata.mention_user_ids` 已设置 |
| 3 | 让 AI "在群里 @12345678 通知一下"（纯数字 ID） | 数字 ID 直接传递，不走 NameResolver |

#### TC-AGENT-006: MessageTool — account_id 自动推断
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | AI 调用 `message` 工具未指定 `account_id` | — |
| 2 | 检查行为 | AccountIdResolver 自动从 bindings 配置推断，写入 `metadata.accountId` |

#### TC-AGENT-007: MessageTool — 非消息渠道防护
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | AI 在 `cli`/`ui`/`employee`/`system` 渠道上下文中尝试使用 `to_name` | — |
| 2 | 检查结果 | 工具返回错误，提示这些参数在非消息渠道不可用 |

#### TC-AGENT-008: sessions_send — 跨 Agent 投递
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工 A 调用 `sessions_send(agentId: "B", message: "请处理此事")` | — |
| 2 | 检查 B 的收件箱 | 消息进入 B 的 inbox，`channel: "employee"`, `chatId: "B"` |
| 3 | 检查 fire-and-forget 语义 | A 不等待 B 的回复，B 通过自身绑定渠道独立响应 |
| 4 | 检查 metadata | `source: "sessions_send"`, `target_agent_id: "B"`, `agent_handoff_from: "A"` |

#### TC-AGENT-009: sessions_send — 同 Agent 自身投递
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工 A 调用 `sessions_send(agentId: "A", message: "...")` | — |
| 2 | 检查结果 | 不走 employee inbox early path，走正常 session 流程 |

#### TC-AGENT-010: ExecTool — 沙箱逃逸防护
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | AI 执行 `cat /etc/passwd`（读操作） | 读取操作正常执行（读操作不拦截） |
| 2 | AI 执行 `echo "test" > /tmp/outside.txt`（写操作到工作空间外） | 写操作被拦截，返回错误 |
| 3 | AI 执行 `cat ../../secret.txt`（路径穿越读） | 读操作放行（只拦截写操作的目标路径） |
| 4 | AI 执行 `cp file.txt ../../outside/`（路径穿越写） | 写操作被拦截 |
| 5 | AI 执行 `rm -rf /`（危险命令） | 被危险命令守卫拦截 |

#### TC-AGENT-011: Spawn — 子智能体上下文传播
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | AI spawn 子智能体执行任务 | — |
| 2 | 检查子智能体上下文 | `originChannel`、`originChatId`、`originSessionKey`、`originAgentId` 正确传递 |
| 3 | 子智能体完成 | 通过系统通道向父会话发送完成通知 |

### 8.2 技能加载与执行（运行时层）

#### TC-SKILL-RUNTIME-001: 技能加载优先级
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工 workspace 中存在同名技能 A（v1.0） | — |
| 2 | additional dirs 中存在同名技能 A（v2.0） | — |
| 3 | builtin 中存在同名技能 A（v3.0） | — |
| 4 | 启动员工引擎 | 加载 workspace 中的 v1.0（workspace 优先级最高） |

#### TC-SKILL-RUNTIME-002: always 技能注入
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 标记一个技能为 `always: true` | — |
| 2 | 启动员工引擎 | 该技能每轮对话都注入到系统提示词 |

#### TC-SKILL-RUNTIME-003: requested_skills 元数据
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 消息中携带 `metadata.requested_skills: ["daily-report"]` | — |
| 2 | 检查技能加载 | 只加载 daily-report 技能，不加载其他技能 |

#### TC-SKILL-RUNTIME-004: 技能依赖检查
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 技能 SKILL.md 声明需要 `python3` 二进制 | — |
| 2 | 环境中存在 python3 | 技能正常加载 |
| 3 | 环境中不存在 python3 | 技能加载时返回依赖缺失错误 |

#### TC-SKILL-RUNTIME-005: PLATFORM_EXCLUDED_SKILLS 排除
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 平台模式下启动员工引擎 | — |
| 2 | 检查可用技能 | `nextclaw-self-manage` 和 `cron` 不在技能列表中 |

#### TC-SKILL-RUNTIME-006: prepareEmployeeRuntime 合并去重
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工绑定了技能 A、B | — |
| 2 | 全局启用了技能 B、C | — |
| 3 | 调用 prepareEmployeeRuntime() | 返回技能集合 {A, B, C}，B 不重复 |

#### TC-SKILL-RUNTIME-007: 日报技能脚本执行
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 让 AI 执行日报填写 | AI 调用 shell 执行 `daily-report.py --query-projects` |
| 2 | 检查 API 调用 | 正确调用内部项管 API（`/admin/oauth2/token` + `/admin/pageProjectForReport`） |
| 3 | 检查文件生成 | `~/nextclaw-temp/daily-report/{weekStart}_{weekEnd}/projects.md` 正确生成 |
| 4 | 让 AI 提交日报 | 调用 `/admin/dayReport` 成功提交 |

#### TC-SKILL-RUNTIME-008: 周报技能脚本执行
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 让 AI 执行周报填写 | AI 调用 shell 执行 `weekly-report.py --mode personal` |
| 2 | 检查数据源 | 读取 `~/nextclaw-temp/daily-report/` 下的 MD 文件 + API 数据 |
| 3 | 检查提交 | 调用 `/admin/week/report` 成功提交 |

#### TC-SKILL-RUNTIME-009: 技能版本在运行时生效
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 升级员工技能版本 | — |
| 2 | 执行该技能 | 使用新版本的文件内容（SKILL.md + scripts） |

### 8.3 身份解析与消息路由

#### TC-IDENTITY-001: IdentityResolver — human_employees 查询
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉消息到达，senderId 为钉钉 external_id | — |
| 2 | 检查解析 | 从 `human_employees` 表（LEFT JOIN departments）查到姓名/部门/职位 |

#### TC-IDENTITY-002: IdentityResolver — users 表 fallback
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉消息到达，senderId 在 human_employees 中不存在 | — |
| 2 | 检查解析 | fallback 到 `users` 表，通过 `external_dingtalk_id` 查到 display_name |

#### TC-IDENTITY-003: buildSenderPrefix — 退化场景
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 用户有完整信息（姓名+部门+职位+用户名） | `[发送者: 张三 (ID:abc123, 用户名:zhangsan, 技术部/高级工程师)]` |
| 2 | 用户只有姓名 | `[发送者: 张三 (ID:abc123)]` |
| 3 | 用户只有姓名和部门 | `[发送者: 张三 (ID:abc123, 技术部)]` |

#### TC-IDENTITY-004: 消息前缀注入到 AI
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 用户 A 发送 "你好" | — |
| 2 | 检查到达 AI 的消息内容 | 内容为 `[发送者: A的姓名 (ID:xxx, 部门/职位)] 你好` |
| 3 | 用户 B 接着发送 "收到" | AI 在对话历史中看到两条带不同前缀的消息 |

#### TC-IDENTITY-005: AgentRouteResolver — 绑定匹配
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉消息到达，channel=dingtalk, accountId=acc1 | — |
| 2 | 检查路由匹配 | 按 channel + accountId + peer 三维匹配到正确的员工 |
| 3 | 无精确匹配 | fallback 到 default agent（matchedBy: "default"） |

#### TC-IDENTITY-006: AgentRouteResolver — dmScope 会话 key
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | dmScope 配置为 `per-peer` | 同一用户不同对话使用不同 session key |
| 2 | dmScope 配置为 `main` | 同一用户所有对话共享一个 session key |
| 3 | dmScope 配置为 `per-channel-peer` | 不同渠道的同一用户使用不同 session key |

#### TC-IDENTITY-007: 群名注册幂等性
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 同一群第一次发消息 | `registerGroup()` 插入记录到 `channel_groups` |
| 2 | 同一群第二次发消息 | 不重复插入（幂等处理） |
| 3 | 检查 channel_groups 表 | 只有一条该群的记录 |

### 8.4 系统提示词构建

#### TC-PROMPT-001: 平台模式提示词
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 平台模式下启动员工 | — |
| 2 | 检查系统提示词 | 包含 "NO CLI available" |
| 3 | 检查 message 工具说明 | 标注仅能向渠道目标发送消息 |
| 4 | 检查 PLATFORM_USAGE.md | 被注入到系统提示词中 |

#### TC-PROMPT-002: 技能摘要 XML 注入
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工绑定了 3 个技能 | — |
| 2 | 每轮对话前检查系统提示词 | 包含 3 个技能的 XML 摘要（名称、描述、可用性状态） |

#### TC-PROMPT-003: 交付上下文注入
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 钉钉群消息到达，包含 thread_ts/msgId | — |
| 2 | 检查 buildDeliveryContext() | `metadata.thread_ts` / `metadata.msgId` 正确注入 |
| 3 | AI 回复时 | 回复消息能正确关联到原消息的线程 |

#### TC-PROMPT-004: IDENTITY.md 注入
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 创建员工时 | 工作空间自动生成 `IDENTITY.md` |
| 2 | 检查 IDENTITY.md 内容 | 包含员工 code、name、description |
| 3 | 检查系统提示词 | IDENTITY.md 内容被注入到身份部分 |

### 8.5 会话管理（运行时层）

#### TC-SESSION-001: 会话创建 vs 复用
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 有 sessionKey 的对话请求 | 查找现有 session，找到则复用 |
| 2 | 有 sessionKey 但 session 不存在 | 按 `createIfMissing` 决定创建或抛错 |
| 3 | 无 sessionKey 的对话请求 | 总是创建新 session（key: `employee:{id}:chat:{uuid}`） |

#### TC-SESSION-002: 对话重置命令
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 发送对话重置命令（如 `/new`） | — |
| 2 | 检查行为 | 创建新 session，后续消息在新会话中 |

#### TC-SESSION-003: agentId 会话隔离 — 工具层
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 员工 A 调用 `sessions_list` | 只返回 `agent:A:` 前缀的会话 |
| 2 | 员工 B 调用 `sessions_list` | 只返回 `agent:B:` 前缀的会话 |
| 3 | 员工 A 调用 `sessions_history` | 只能查看 `agent:A:` 前缀的会话历史 |

#### TC-SESSION-004: sessions_send — handoff depth 限制
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | A → B → C → D 连续 handoff（depth=3） | 消息正常传递 |
| 2 | 超过 maxPingPongTurns 限制 | 停止 handoff，返回错误 |

### 8.6 子智能体行为

#### TC-SUBAGENT-001: 子智能体工具限制
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 检查子智能体可用工具 | 不包含 `message`、`cron`、`gateway` 工具 |
| 2 | 子智能体尝试调用 message 工具 | 工具不存在，调用失败 |

#### TC-SUBAGENT-002: 子智能体完成通知
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 子智能体完成任务 | — |
| 2 | 检查父会话 | 收到系统消息通知，包含子智能体完成结果 |

### 8.7 NO_PROXY 代理过滤

#### TC-PROXY-001: NO_PROXY 内网地址直连
| 步骤 | 操作 | 预期结果 |
|---|---|---|
| 1 | 配置 NO_PROXY 包含内网地址 | — |
| 2 | 员工引擎访问内网地址 | 不走代理，直接连接 |
| 3 | 员工引擎访问外网地址 | 走代理 |

---

## 九、测试用例统计与优先级分布

| 优先级 | 测试类别 | 用例数量 | 用例编号范围 |
|---|---|---|---|
| P0 | 用户认证与权限 | 8 | TC-AUTH-001 ~ 008 |
| P0 | 人员同步 | 5 | TC-SYNC-001 ~ 005 |
| P0 | 技能版本升级（UI） | 10 | TC-SKILL-001 ~ 010 |
| P0 | 智能体工具调用 | 11 | TC-AGENT-001 ~ 011 |
| P0 | 技能加载与执行 | 9 | TC-SKILL-RUNTIME-001 ~ 009 |
| P0 | 数据库迁移 | 3 | TC-DB-001 ~ 003 |
| P1 | 聊天模块 | 9 | TC-CHAT-001 ~ 009 |
| P1 | 身份解析与消息路由 | 7 | TC-IDENTITY-001 ~ 007 |
| P1 | 工作空间安全 | 5 | TC-WORKSPACE-001 ~ 005 |
| P1 | 系统提示词构建 | 4 | TC-PROMPT-001 ~ 004 |
| P1 | Webhook 触发器 | 3 | TC-WEBHOOK-001 ~ 003 |
| P1 | 用户管理 | 4 | TC-USER-001 ~ 004 |
| P2 | 会话管理 | 4 | TC-SESSION-001 ~ 004 |
| P2 | 子智能体行为 | 2 | TC-SUBAGENT-001 ~ 002 |
| P2 | 钉钉渠道 | 2 | TC-DINGTALK-001 ~ 002 |
| P2 | 定时任务 | 3 | TC-SCHEDULE-001 ~ 003 |
| P2 | Docker/部署 | 3 | TC-DEPLOY-001 ~ 003 |
| P2 | 日报/周报技能 | 5 | TC-REPORT-001 ~ 005 |
| P2 | NO_PROXY 代理过滤 | 1 | TC-PROXY-001 |
| **合计** | | **96** | |
